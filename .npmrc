# All @napgram/* packages now live in this monorepo as workspace packages,
# so no private GitHub Packages registry/token is required.
registry=https://registry.npmjs.org/

# Allow recently published packages
minimumReleaseAge=0
